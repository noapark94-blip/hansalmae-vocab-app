/* Friends and server-authoritative battles; rewards are settled atomically by the server. */
(function(){
 'use strict';
 let root, home=null, game=null, owner='', busy=false, pollBusy=false, lastPoll=0, signature='', offset=0, modal=null, searchResult=null, revision=0, lastRewardSync='';
 const $=id=>document.getElementById(id), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const who=()=>typeof currentStudent!=='undefined'&&currentStudent?String(currentStudent.studentId||''):'';
 const active=()=>root&&!root.classList.contains('hidden');
 const live=()=>game&&['invited','ready','playing'].includes(game.status);
 const modes={engToKor:'영어 → 한글',korToEng:'한글 → 영어',mixed:'영한·한영 혼합'};
 const icon='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true"><path d="m4 3 6 6-3 3-6-6V3Zm16 0-6 6 3 3 6-6V3ZM8 14l-5 5m13-5 5 5M3 15l6 6m6 0 6-6M9 9l10 10M15 9 5 19"/></svg>';
 const button=(label,action,id='',cls='')=>'<button type="button" class="fb-btn '+cls+'" data-action="'+action+'" data-id="'+esc(id)+'">'+label+'</button>';
 function image(p){let src=String(p?.image||'');if(!/^\.\/images\/|^https:\/\//.test(src))src='./images/emblems/title-chick.png';return '<img class="fb-avatar" src="'+esc(src)+'" alt="" loading="lazy">';}
 function person(p){return '<div class="fb-person">'+image(p)+'<div><strong>'+esc(p?.name||'친구')+'</strong><small>Lv.'+esc(p?.level||1)+' · '+esc(p?.title||'단어병아리')+'</small></div></div>';}
 function questionCount(s){return [10,20,30].includes(Number(s?.count))?Number(s.count):10;}
 function timeLimit(s){return [10,15,20].includes(Number(s?.seconds))?Number(s.seconds):10;}
 function bookTitle(s){return String(s||'').replace(/^(중등|고등|수능)단어\s*DB$/i,'$1단어');}
 function summary(s){return esc(bookTitle(s.title))+(s.kind==='school'?'':' · Day '+s.start+'–'+s.end)+' · '+esc(modes[s.mode]||'')+' · '+questionCount(s)+'문제 · 문제당 '+timeLimit(s)+'초';}
 function toast(msg){const el=$('fbToast');el.textContent=msg;el.hidden=false;clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.hidden=true,3500);}
 async function api(action,payload={}){
  const account=who();if(!account)throw Error('로그인 후 이용해주세요.');
  const token=await window.hsmEnsureStudentSession_(false);
  if(account!==who())throw Error('로그인 계정이 변경됐어요.');
  const ctl=new AbortController(),timer=setTimeout(()=>ctl.abort(),15000);
  try{const res=await fetch(window.HANSALMAE_CONFIG.apiUrl.replace(/\/api\/?$/,'/word-battle'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,action,payload}),signal:ctl.signal});
   const data=await res.json();if(account!==who())throw Error('로그인 계정이 변경됐어요.');if(!res.ok||!data.success)throw Error(data.message||'잠시 후 다시 시도해주세요.');return data.result;
  }finally{clearTimeout(timer);}
 }
 function stamp(g){const previous=game;game=g;offset=new Date(g.serverNow).getTime()-Date.now();renderGame();if(g.status==='finished'&&g.reward&&(g.reward.xp||g.reward.points)&&lastRewardSync!==g.id){lastRewardSync=g.id;try{localStorage.removeItem('hsmMonthlyRankingCacheV2');}catch(_){}window.refreshExperienceUi_?.();window.HSMEmblems?.load(true);}if(previous?.id===g.id)for(const side of ['host','guest'])if(g[side+'Score']>previous[side+'Score']){const score=root.querySelector('[data-score="'+side+'"]');score?.classList.add('fb-pop');const fighter=root.querySelector('.fb-fighter.fb-'+side);if(fighter){const gain=document.createElement('span');gain.className='fb-score-gain';gain.textContent='+1';fighter.appendChild(gain);setTimeout(()=>gain.remove(),1000);}}}
 async function refresh(force=false){
  if(pollBusy||busy||!who()||document.hidden||!$('mainApp')||$('mainApp').classList.contains('hidden'))return;
  if(!force&&Date.now()-lastPoll<(active()&&live()?1400:active()?7000:20000))return;
  pollBusy=true;lastPoll=Date.now();const version=revision,account=who(),room=game?.id;
  try{if(active()&&game){const data=await api('poll',{id:room});if(version===revision&&account===who()&&game?.id===room)stamp(data);}else {const data=await api('home');if(version===revision&&account===who()){home=data;updateBadge();if(active()&&!game)renderHome();}}const status=$('fbConnection');if(status)status.textContent='';}
  catch(e){if(active()){const status=$('fbConnection');if(status)status.textContent='연결을 확인하고 있어요. 잠시만 기다려주세요.';if(force)toast(e.message);}}
  finally{pollBusy=false;}
 }
 function updateBadge(){const requests=(home?.friends||[]).filter(f=>f&&f.status==='pending'&&f.incoming).length;const invites=(home?.battles||[]).filter(b=>b.status==='invited'&&b.guest===home.me.id).length;const notice=$('fbInviteNotice'),incoming=(home?.battles||[]).find(b=>b.status==='invited'&&b.guest===home.me.id);if(notice){const taking=['testScreen','teacherExamTakingScreen'].some(id=>$(id)&&!$(id).classList.contains('hidden'))||document.querySelector('.hsm-school-test-overlay:not([hidden])');notice.hidden=!incoming||active()||!!taking||$('mainApp').classList.contains('hidden');if(incoming){notice.textContent=(incoming.opponent?.name||'친구')+' 님의 대전 신청 · 확인하기';notice.dataset.id=incoming.id;}}const badge=$('fbShortcutCount');if(badge){badge.hidden=!(invites||requests);badge.textContent=invites?'대전 신청 '+invites+'개':requests?'친구 요청 '+requests+'개':'';}}
 async function open(){
  if(!who())return;owner=who(); if(typeof window.hsmRememberCurrentScreen_==='function')window.hsmRememberCurrentScreen_();
  if(typeof window.hsmUnmountSchoolStudy_==='function')window.hsmUnmountSchoolStudy_();
  window.hideAllStudentMainScreens_();root.classList.remove('hidden');if($('fbInviteNotice'))$('fbInviteNotice').hidden=true;window.setActiveMenu('mypage');window.scrollTo(0,0);signature='';
  if(game)renderGame();else renderHome();await refresh(true);
 }
 function matchCard(b){
  const incoming=b.status==='invited'&&b.guest===home.me.id;
  const status=b.status==='playing'?'대전 진행 중':b.status==='ready'?'준비 중':incoming?'대전 신청 도착':'수락 대기 중';
  const player=(p,label)=>'<div class="fb-lobby-player"><small>'+label+'</small>'+image(p)+'<strong>'+esc(p?.name||'나')+'</strong></div>';
  const settings=b.settings||{};
  return '<article class="fb-match-card"><div class="fb-match-top"><span class="fb-live-state">'+status+'</span><span>1 : 1 MATCH</span></div><div class="fb-match-players">'+player(home.me,'YOU')+'<b class="fb-lobby-vs">VS</b>'+player(b.opponent,'RIVAL')+'</div><div class="fb-match-chips"><span>'+esc(bookTitle(settings.title))+'</span>'+(settings.kind==='school'?'':'<span>Day '+esc(settings.start)+'–'+esc(settings.end)+'</span>')+'<span>'+questionCount(settings)+'문제</span><span>'+timeLimit(settings)+'초</span><span>'+esc(modes[settings.mode]||'')+'</span></div>'+button(incoming?'대전 신청 확인':b.status==='invited'?'대기실 입장':'대전 입장','room',b.id,'fb-match-enter')+'</article>';
 }
 function renderHome(){
  root.classList.remove('fb-game','fb-finished');root.classList.add('fb-lobby');
  const focused=document.activeElement;if(focused&&root.contains(focused)&&focused.matches('input'))return;
  const searchOpen=!!root.querySelector('.fb-add-friend[open]'),searchValue=$('fbStudentId')?.value||'';
  const rulesOpen=!!root.querySelector('.fb-rules[open]');
  const friends=(home?.friends||[]).filter(Boolean),accepted=friends.filter(f=>f.status==='accepted'),pending=friends.filter(f=>f.status==='pending');
  const rooms=(home?.battles||[]).filter(b=>['invited','ready','playing'].includes(b.status));
  const content=JSON.stringify([home,searchResult]);if(signature===content)return;signature=content;
  root.innerHTML='<header class="fb-lobby-banner"><div class="fb-lobby-title"><h2>WORD<br>BATTLE</h2></div></header><div id="fbConnection" class="fb-connection" role="status"></div>'+
   (rooms.length?'<div class="fb-section-head"><h3>나의 대전 <span>'+rooms.length+'</span></h3><span class="fb-section-caption">READY TO PLAY</span></div>'+rooms.map(matchCard).join(''):'<p class="fb-lobby-welcome">함께 겨룰 친구를 선택해보세요.</p>')+
   '<div class="fb-section-head"><h3>내 친구 <span>'+accepted.length+'</span></h3>'+'<div class="fb-lobby-tools"><button type="button" class="fb-btn fb-add-toggle" data-action="toggleSearch" aria-controls="fbAddFriend" aria-expanded="'+searchOpen+'"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="9" cy="8" r="3.2"/><path d="M3 20v-2a6 6 0 0 1 12 0v2M19 8v6m-3-3h6"/></svg><span>친구 추가</span></button>'+button('<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" aria-hidden="true"><path d="M4 7h7m6 0h3M4 17h3m6 0h7"/><circle cx="14" cy="7" r="3"/><circle cx="10" cy="17" r="3"/></svg><span>관리</span>','manage','','fb-manage-toggle')+'</div></div>'+
   '<details id="fbAddFriend" class="fb-add-friend" '+(searchOpen?'open':'')+'><summary hidden>친구 추가</summary><form id="fbSearch" class="fb-search"><label for="fbStudentId">학생 아이디로 친구 찾기</label><div><input id="fbStudentId" maxlength="80" autocomplete="off" placeholder="친구의 정확한 학생 아이디" value="'+esc(searchValue)+'" required minlength="2">'+button('찾기','search')+'</div></form>'+
   (searchResult?'<div class="fb-row">'+person(searchResult)+(friends.some(f=>f.id===searchResult.id)?'<span class="fb-muted">이미 요청했거나 친구예요</span>':button('친구 요청','request',searchResult.id,'fb-soft'))+'</div>':'')+'</details>'+
   (pending.length?'<h3>친구 요청 <span>'+pending.length+'</span></h3>'+pending.map(f=>'<div class="fb-row">'+person(f)+'<div class="fb-actions">'+(f.incoming?button('수락','acceptFriend',f.id,'fb-soft')+button('거절','declineFriend',f.id,'fb-text'):button('요청 취소','declineFriend',f.id,'fb-text'))+'</div></div>').join(''):'')+
   '<div class="fb-friend-list">'+(accepted.length?accepted.sort((a,b)=>Number(b.online)-Number(a.online)).map(f=>'<div class="fb-friend">'+person(f)+'<div class="fb-friend-bottom"><span class="fb-presence '+(f.busy?'is-busy':f.online?'is-online':'')+'">'+(f.busy?'대전 중':f.online?'접속 중':'오프라인')+'</span>'+button(icon+'<span>대전 신청</span>','challenge',f.id,'fb-soft')+'</div></div>').join(''):'<div class="fb-empty">'+image(null)+'<strong>첫 대전 친구를 만나보세요</strong><p>친구 추가에서 아이디를 검색하고<br>서로 수락하면 대전할 수 있어요.</p></div>')+'</div>'+
   ((home?.battles||[]).some(b=>b.status==='finished')?'<div class="fb-section-head"><h3>최근 대전</h3><span class="fb-section-caption">MATCH HISTORY</span></div><div class="fb-history-list">'+home.battles.filter(b=>b.status==='finished').slice(0,5).map(b=>{const outcome=!b.winner?'draw':b.winner===home.me.id?'win':'lose';return '<button type="button" class="fb-history" data-action="room" data-id="'+esc(b.id)+'">'+image(b.opponent)+'<span class="fb-history-name">'+esc(b.opponent?.name||'친구')+'</span><span class="fb-history-result is-'+outcome+'">'+outcome.toUpperCase()+'</span><span aria-hidden="true">›</span></button>';}).join('')+'</div>':'')+
   '<details class="fb-rules" '+(rulesOpen?'open':'')+'><summary>대전 규칙 안내</summary><p>정답 수로 승부하고, 동점이면 정답을 맞힌 총 시간으로 결정해요.</p><p>10문제 기준 WIN +5P · DRAW +3P · LOSE +1P. 경험치는 정답 1개당 +2 EXP, 완료 시 +5 EXP예요. 20·30문제는 포인트와 완료 경험치가 2·3배예요.</p><p>기존 월간 포인트와 누적 경험치에 합산돼요. 하루 100문제, 같은 친구와 하루 첫 3판까지 보상하며, 제한 시간에 따른 차이는 없어요. 한도가 남은 만큼 앞 문제부터 계산하고 비례 보상은 소수점 아래를 버려요. 날짜는 한국 시간 자정에 바뀌어요.</p><p>일반 단어장은 해당 단어장 순위에, 학교 수행평가는 학생 학년에 맞는 중등·고등 순위에 합산돼요. 중도 포기자는 보상이 없고, 상대는 이미 채점된 정답의 경험치만 받아요.</p></details>';
  }
 function scoreboard(g){
  if(g.status==='finished'){
   const player=(p,side)=>'<div class="fb-stage-player fb-'+side+(g.winner===p?.id?' is-winner':'')+'"><span class="fb-player-tag">'+(p?.id===g.me?'YOU':'RIVAL')+'</span>'+image(p)+'</div>';
   const plate=(p,side)=>'<div class="fb-nameplate"><strong>'+esc(p?.name)+'</strong><small><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true"><circle cx="12" cy="12" r="8"/><path d="M12 7v5l3 2"/></svg>'+((side==='host'?g.hostMs:g.guestMs)/1000).toFixed(1)+'<span>초</span></small></div>';
   return '<div class="fb-final-arena"><div class="fb-final-stage">'+player(g.host,'host')+'<div class="fb-final-score"><span>FINAL</span><div class="fb-score"><b data-score="host">'+g.hostScore+'</b><span>:</span><b data-score="guest">'+g.guestScore+'</b></div><small>정답 수</small></div>'+player(g.guest,'guest')+'</div><div class="fb-nameplates">'+plate(g.host,'host')+plate(g.guest,'guest')+'</div></div>';
  }

  const fighter=(p,side)=>{const mine=p?.id===g.me,done=mine?g.answered:g.opponentAnswered;const status=g.status==='finished'?((side==='host'?g.hostMs:g.guestMs)/1000).toFixed(1)+'초':g.status==='playing'?(g.revealUntil?'ROUND RESULT':!g.question?'READY':done?'선택 완료 ✓':'고르는 중…'):g.status==='ready'?((side==='host'?g.hostReady:g.guestReady)?'준비 완료 ✓':'준비 중'):mine?'PLAYER 1 · 나':'PLAYER 2 · 친구';return '<div class="fb-fighter fb-'+side+(g.status==='finished'&&g.winner===p?.id?' is-winner':'')+'"><span class="fb-player-tag">'+(mine?'YOU':'RIVAL')+'</span>'+image(p)+'<strong>'+esc(p?.name)+'</strong><small class="'+(done?'is-done':'')+'">'+status+'</small></div>';};
  return '<div class="fb-scoreboard fb-arena">'+fighter(g.host,'host')+'<div class="fb-score-center"><span class="fb-versus">'+(g.status==='finished'?'FINAL':'VS')+'</span><div class="fb-score"><b data-score="host">'+g.hostScore+'</b><span>:</span><b data-score="guest">'+g.guestScore+'</b></div><small>'+(g.status==='finished'?'정답 수':'WORD BATTLE')+'</small></div>'+fighter(g.guest,'guest')+'</div>';
 }
 function rewardPanel(g){
  const r=g.reward;
  if(!r)return '<p class="fb-reward-note">'+(g.rewardsEnabled?'보상을 확인하고 있어요. 잠시 후 다시 확인해주세요.':'보상 제도 적용 전 대전이에요.')+'</p>';
  const notes={awarded:'기존 월간 포인트와 누적 경험치에 반영됐어요.',partial:'하루 한도에 따라 앞 '+r.eligibleCount+'문제의 보상이 반영됐어요.',pair_limit:'같은 친구와 하루 3판 보상을 모두 받았어요.',daily_limit:'오늘의 100문제 보상을 모두 받았어요.',forfeit:'중도 포기로 이번 대전의 보상은 없어요.',opponent_left:'상대가 나가서 이미 채점된 정답의 경험치만 받았어요.',no_rounds:'채점된 문제가 없어 이번 대전의 보상은 없어요.'};
  const progress=Math.max(0,Math.min(100,Number(r.progress)||0));
  return '<section class="fb-rewards" aria-label="대전 보상"><div class="fb-reward-values"><div><small>월간 포인트</small><strong>+'+esc(r.points)+'<em>P</em></strong></div><div><small>획득 경험치</small><strong>+'+esc(r.xp)+'<em>EXP</em></strong></div></div>'+
   '<div class="fb-reward-level"><span>Lv.'+esc(r.level)+' · '+esc(r.title)+'</span><span>'+(r.level>r.previousLevel?'LEVEL UP!':r.nextLevelXp==null?'MAX LEVEL':'다음 레벨까지 '+Math.max(0,r.nextLevelXp-r.totalXp)+' EXP')+'</span></div><div class="fb-reward-track" role="progressbar" aria-label="레벨 경험치" aria-valuemin="0" aria-valuemax="100" aria-valuenow="'+progress+'"><i style="--reward-progress:'+progress+'%"></i></div><p class="fb-reward-note">'+esc(notes[r.reason]||'보상 내역을 확인해주세요.')+'</p></section>';
 }
 function updateRewardPreview(){
  const info=modal?._rewardPreview,el=$('fbRewardPreview');if(!el)return;
  const count=Number(modal.querySelector('[data-action="count"][aria-pressed="true"]')?.dataset.id)||10;
  let note='WIN +'+count/10*5+'P · DRAW +'+count/10*3+'P · LOSE +'+count/10+'P';
  if(info&&Number.isFinite(Number(info.remaining))){if(info.pairRemaining<=0)note='친선전 · 이 친구와 오늘의 3판 보상을 모두 받았어요.';else if(info.remaining<=0)note='친선전 · 오늘의 100문제 보상을 모두 받았어요.';else note+=' · 오늘 '+info.remaining+'문제 남음'+(info.remaining<count?' (앞 '+info.remaining+'문제만 보상)':'');}
  el.textContent=note;
 }
 function resultHeading(g){
  const outcome=!g.winner?'draw':g.winner===g.me?'win':'lose';
  let detail=g.reason||(!g.winner?'정답 수와 풀이 시간이 같아요.':g.winner===g.me?'멋진 한 판, 승리는 나의 것!':'좋은 승부였어요. 다시 도전해볼까요?');
  if(!g.reason&&g.winner&&g.hostScore===g.guestScore){
   const delta=Math.abs(g.hostMs-g.guestMs)/1000;
   detail='정답 수 동점 · '+(delta<0.1?'0.1초 미만':delta.toFixed(1)+'초')+' 차이로 결정됐어요.';
  }
  return '<div class="fb-result-heading is-'+outcome+'"><span>MATCH COMPLETE</span><h3>'+outcome.toUpperCase()+'</h3><p>'+esc(detail)+'</p></div>';
 }
 function renderGame(){
  if(!active()||!game)return;const g=game,content=JSON.stringify({...g,serverNow:null,deadline:g.deadline});if(content===signature){tick();return;}signature=content;root.classList.remove('fb-lobby');root.classList.add('fb-game');root.classList.toggle('fb-finished',g.status==='finished');
  const mine=g.host?.id===g.me,ready=mine?g.hostReady:g.guestReady;
  let html='<div class="fb-section-head"><h2 class="fb-game-title">WORD BATTLE</h2>'+(g.status==='finished'?'':button(live()?'나가기':'친구 목록','exit','','fb-text'))+'</div><p class="fb-sub">'+summary(g.settings)+'</p><div id="fbConnection" class="fb-connection" role="status"></div>'+(g.status==='finished'?resultHeading(g):'')+scoreboard(g);
  if(g.status==='invited')html+='<div class="fb-wait"><span class="fb-orbit">'+icon+'</span><h3>'+(mine?'친구의 수락을 기다려요':'친구가 대전을 신청했어요')+'</h3><p>한 문제에 '+timeLimit(g.settings)+'초 · 총 '+questionCount(g.settings)+'문제<br>서로 준비되면 함께 시작해요.</p></div>'+(mine?'<p class="fb-note">2분 안에 수락하지 않으면 신청이 종료돼요.</p>':'<div class="fb-two">'+button('다음에 할게요','decline',g.id)+button('함께 대전하기','accept',g.id,'fb-primary')+'</div>');
  else if(g.status==='ready')html+='<div class="fb-wait"><h3>준비됐나요?</h3><p>같은 문제, 각자의 실력.<br>둘 다 준비를 누르면 시작해요.</p><div class="fb-readiness"><span>'+(g.hostReady?'✓':'○')+' '+esc(g.host?.name)+'</span><span>'+(g.guestReady?'✓':'○')+' '+esc(g.guest?.name)+'</span></div></div><button class="fb-btn fb-primary fb-wide" data-action="ready" '+(ready?'disabled':'')+'>'+(ready?'친구의 준비를 기다려요':'준비 완료')+'</button>';
  else if(g.status==='playing'){
   const q=g.question;
   html+='<div class="fb-round"><span><small>ROUND</small> '+String(g.round+1).padStart(2,'0')+' / '+questionCount(g.settings)+'</span><span id="fbTimer" aria-live="off"></span></div><div class="fb-progress fb-time-track"><i id="fbTimeFill"></i></div>';
   if(!q)html+='<div class="fb-countdown"><span id="fbCountdown"></span><p>곧 시작해요. 집중해보세요!</p></div>';
   else html+='<p class="fb-prompt-label">'+(q.mode==='engToKor'?'알맞은 뜻을 골라주세요':'알맞은 영어를 골라주세요')+'</p><h3 class="fb-question">'+esc(q.prompt)+'</h3><div class="fb-options">'+q.options.map((o,i)=>'<button class="fb-option '+(q.correctId===o.id?'is-correct':g.choice===o.id?(q.correctId?'is-wrong':'is-picked'):'')+'" data-action="answer" data-id="'+esc(o.id)+'" '+(g.answered||q.correctId?'disabled':'')+'><span>'+String(i+1).padStart(2,'0')+'</span><b>'+esc(o.text)+'</b>'+(q.correctId===o.id?'<em aria-label="정답">✓</em>':g.choice===o.id?'<em aria-label="'+(q.correctId?'오답':'선택 완료')+'">'+(q.correctId?'×':'✓')+'</em>':'')+'</button>').join('')+'</div><p class="fb-answer-status" role="status">'+(q.correctId?(g.choice===q.correctId?'정답이에요!':'정답을 확인해보세요.') : g.answered?(g.opponentAnswered?'결과를 확인하고 있어요':'제출 완료 · 친구의 답을 기다려요'):g.opponentAnswered?'친구가 답을 제출했어요.':'정답을 고르면 바로 제출돼요.')+'</p>';
  }else if(g.status==='finished'){
   const score=mine?g.hostScore:g.guestScore,ms=mine?g.hostMs:g.guestMs,wrong=(g.review||[]).filter(w=>!w.correct);
   html+='<div class="fb-result"><div class="fb-result-stats"><div><small>내 정답</small><strong>'+score+'<em> / '+questionCount(g.settings)+'</em></strong></div><div><small>정답 풀이 시간</small><strong>'+(ms/1000).toFixed(1)+'<em>초</em></strong></div></div></div>'+rewardPanel(g)+'<div class="fb-two fb-result-actions">'+button('<span aria-hidden="true">←</span> 뒤로가기','exit')+button('<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M4 10a8 8 0 1 1 1 7M4 4v6h6"/></svg>한 판 더','rematch','','fb-primary')+'</div>'+(wrong.length?'<details class="fb-review"><summary>다시 익힐 단어 <span>'+wrong.length+'개</span></summary>'+wrong.map(w=>'<article><strong>'+esc(w.word)+'</strong><p>'+esc(w.meaning)+'</p>'+(w.example?'<div>'+esc(w.example)+(w.translation?'<small>'+esc(w.translation)+'</small>':'')+'</div>':'')+'</article>').join('')+'</details>':'<p class="fb-review-clear"><span aria-hidden="true">✓</span> 다시 익힐 단어가 없어요</p>');

  }else html+='<div class="fb-wait"><h3>대전이 종료됐어요</h3><p>'+esc(g.reason||(g.status==='declined'?'친구가 이번 신청을 사양했어요.':'다음에 다시 함께해요.'))+'</p></div>'+button('친구 목록으로','exit','','fb-primary fb-wide');
  root.innerHTML=html;tick();
 }
 function tick(){if(!active()||game?.status!=='playing')return;const now=Date.now()+offset,remaining=Math.max(0,(new Date(game.deadline)-now)/1000),waiting=now<new Date(game.roundAt);const timer=$('fbTimer'),count=$('fbCountdown'),fill=$('fbTimeFill');if(timer){timer.textContent=game.revealUntil?'정답 확인':waiting?'READY':Math.ceil(remaining)+'초';timer.classList.toggle('is-urgent',!waiting&&!game.revealUntil&&remaining<=3);}if(fill){fill.style.width=(game.revealUntil?0:Math.min(100,remaining/timeLimit(game.settings)*100))+'%';fill.classList.toggle('is-urgent',!waiting&&!game.revealUntil&&remaining<=3);}if(count){const value=waiting?String(Math.max(1,Math.ceil((new Date(game.roundAt)-now)/1000))):'START';if(count.textContent!==value){count.textContent=value;count.classList.remove('fb-pop');void count.offsetWidth;count.classList.add('fb-pop');}}}
 function closeModal(){if(!modal)return;const back=modal._back;modal.remove();modal=null;document.body.style.overflow=closeModal.overflow||'';if(back?.isConnected)back.focus({preventScroll:true});}
 function sheet(title,body){closeModal();const m=document.createElement('div');m.className='fb-overlay';m.innerHTML='<section class="fb-sheet" role="dialog" aria-modal="true" aria-labelledby="fbModalTitle" tabindex="-1"><div class="fb-section-head"><h2 id="fbModalTitle">'+esc(title)+'</h2>'+button('×','close','','fb-close')+'</div>'+body+'</section>';m.querySelector('[data-action="close"]').setAttribute('aria-label','닫기');m._back=document.activeElement;closeModal.overflow=document.body.style.overflow;document.body.style.overflow='hidden';document.body.appendChild(m);modal=m;m.addEventListener('click',e=>{if(e.target===m)closeModal();else handle(e);});m.addEventListener('keydown',e=>{if(e.key==='Escape'){e.preventDefault();if(!closeDayPicker())closeModal();}if(e.key==='Tab'){const nodes=[...m.querySelectorAll('button:not(:disabled),input,select')].filter(n=>n.getClientRects().length);const first=nodes[0],last=nodes.at(-1);if(e.shiftKey&&(document.activeElement===first||document.activeElement===m.firstElementChild)){e.preventDefault();last?.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first?.focus();}}});m.firstElementChild.focus({preventScroll:true});}
 function closeDayPicker(){const panel=modal?.querySelector('.fb-day-picker');if(!panel)return false;const id=panel.dataset.target;panel.remove();modal.firstElementChild.classList.remove('fb-day-open');modal.querySelector('[data-action="day"][data-id="'+id+'"]').focus({preventScroll:true});return true;}
 function syncDays(){for(const id of ['fbStart','fbEnd'])modal.querySelector('[data-action="day"][data-id="'+id+'"]').innerHTML='Day '+esc($(id).value)+' <span aria-hidden="true">⌄</span>';}
 function showDayPicker(id){const select=$(id);if(!select)return;const panel=document.createElement('div');panel.className='fb-day-picker';panel.dataset.target=id;panel.innerHTML='<div class="fb-section-head"><h2>'+(id==='fbStart'?'시작 Day':'마지막 Day')+' 선택</h2>'+button('×','dayBack','','fb-close')+'</div><p class="fb-sub">시작과 마지막 Day를 포함해 출제해요.</p><div class="fb-day-current"><span>현재 대전 범위</span><strong>Day '+esc($('fbStart').value)+' <i>—</i> '+esc($('fbEnd').value)+'</strong></div><div class="fb-day-grid">'+[...select.options].map(o=>'<button type="button" class="fb-btn '+(Number(o.value)>=Number($('fbStart').value)&&Number(o.value)<=Number($('fbEnd').value)?'in-range':'')+'" data-action="pickDay" data-id="'+o.value+'" aria-pressed="'+(o.value===select.value)+'">'+o.value+'</button>').join('')+'</div>';panel.querySelector('[data-action="dayBack"]').setAttribute('aria-label','Day 선택 닫기');modal.firstElementChild.classList.add('fb-day-open');modal.firstElementChild.appendChild(panel);panel.querySelector('[aria-pressed="true"]')?.focus({preventScroll:true});panel.querySelector('[aria-pressed="true"]')?.scrollIntoView?.({block:'nearest'});}
 async function challenge(id){const [items,rewardPreview]=await Promise.all([api('catalog',{target:id}),api('rewardPreview',{target:id}).catch(()=>null)]);if(!items.length)throw Error('함께 이용할 수 있는 단어장이 없어요.');const p=home?.friends?.find(f=>f?.id===id)||(game&&(game.host.id===id?game.host:game.guest));
  const choices=(label,action,values,current)=>'<fieldset class="fb-count-field"><legend>'+label+'</legend><div class="fb-count-options">'+values.map(([value,text])=>'<button type="button" class="fb-btn" data-action="'+action+'" data-id="'+value+'" aria-pressed="'+(value===current)+'">'+text+'</button>').join('')+'</div></fieldset>';
  sheet('대전 준비','<form id="fbChallenge"><label for="fbSource" class="fb-source-label">단어장</label><select id="fbSource" hidden tabindex="-1" aria-hidden="true">'+items.map(x=>'<option value="'+esc(x.id)+'">'+esc(bookTitle(x.title))+'</option>').join('')+'</select><div class="fb-source-cards" role="group" aria-label="단어장 선택">'+items.map((x,i)=>'<button type="button" class="fb-btn" data-action="source" data-id="'+esc(x.id)+'" aria-pressed="'+(i===0)+'"><strong>'+esc(bookTitle(x.title))+'</strong></button>').join('')+'</div><fieldset id="fbRange" class="fb-count-field"><legend>대전 범위</legend><select id="fbStart" hidden aria-hidden="true" tabindex="-1"></select><select id="fbEnd" hidden aria-hidden="true" tabindex="-1"></select><div class="fb-day-range">'+button('Day 1','day','fbStart')+'<span aria-hidden="true">→</span>'+button('Day 1','day','fbEnd')+'</div></fieldset>'+choices('문제 유형','mode',[['engToKor','영→한'],['korToEng','한→영'],['mixed','혼합']],'mixed')+choices('문제 수','count',[[10,'10문제'],[20,'20문제'],[30,'30문제']],10)+choices('문제당 제한 시간','seconds',[[10,'10초'],[15,'15초'],[20,'20초']],10)+'<div class="fb-challenge-rules">정답 수가 같으면 더 빠르게 맞힌 사람이 승리해요.<br>정답당 +2 EXP · 완료 보너스 10문제당 +5 EXP<br>하루 100문제 · 같은 친구 첫 3판까지 보상</div><p id="fbRewardPreview" class="fb-reward-preview"></p><p class="fb-challenge-hint">친구가 수락하고 둘 다 준비하면 시작해요.</p>'+button('대전 신청 <span aria-hidden="true">→</span>','invite',id,'fb-primary fb-wide')+'</form>');modal.firstElementChild.classList.add('fb-challenge-sheet');modal._rewardPreview=rewardPreview;updateRewardPreview();
  const fill=()=>{const s=items.find(i=>i.id===$('fbSource').value);$('fbRange').hidden=s.kind==='school';const days=s.days||[];for(const key of ['fbStart','fbEnd'])$(key).innerHTML=days.map(d=>'<option value="'+Number(d)+'">Day '+Number(d)+'</option>').join('');if(days.length)$('fbEnd').value=days[days.length-1];syncDays();};$('fbSource').addEventListener('change',fill);fill();}
 async function exitGame(){if(live()){if(!await HSMDialog.confirm({title:'대전을 나갈까요?',message:game.status==='playing'?'지금 나가면 LOSE로 기록되고 포인트와 경험치를 받을 수 없어요.':'대전 신청이 종료돼요.',action:'나가기',cancel:'계속하기'}))return false;await api('leave',{id:game.id});}game=null;signature='';home=await api('home');renderHome();updateBadge();return true;}
 async function handle(e){const el=e.target.closest('[data-action]');if(!el)return;e.preventDefault();if(busy)return;const a=el.dataset.action,id=el.dataset.id;
  if(a==='toggleSearch'){const panel=$('fbAddFriend');panel.open=!panel.open;el.setAttribute('aria-expanded',String(panel.open));if(panel.open)$('fbStudentId').focus({preventScroll:true});return;}if(a==='close'){closeModal();return;}if(a==='source'){$('fbSource').value=id;$('fbSource').dispatchEvent(new Event('change'));modal.querySelectorAll('[data-action="source"]').forEach(b=>b.setAttribute('aria-pressed',String(b===el)));return;}if(['count','mode','seconds'].includes(a)){modal.querySelectorAll('[data-action="'+a+'"]').forEach(b=>b.setAttribute('aria-pressed',String(b===el)));updateRewardPreview();return;}if(a==='day'){showDayPicker(id);return;}if(a==='dayBack'){closeDayPicker();return;}if(a==='pickDay'){const target=modal.querySelector('.fb-day-picker').dataset.target;$(target).value=id;if(Number($('fbStart').value)>Number($('fbEnd').value))$(target==='fbStart'?'fbEnd':'fbStart').value=id;syncDays();closeDayPicker();return;}busy=true;revision++;el.disabled=true;
  try{
   if(a==='search'){const input=$('fbStudentId');if(!input.reportValidity())return;const r=await api('search',{search:input.value.trim()});searchResult=r.person;input.blur();signature='';renderHome();if(!r.person)toast('아이디를 다시 확인해주세요.');}
   else if(['request','acceptFriend','declineFriend','remove','block','unblock'].includes(a)){
    if(['remove','block'].includes(a)&&!await HSMDialog.confirm(a==='block'?'이 친구의 요청과 대전 신청을 받지 않을까요?':'친구 목록에서 삭제할까요?'))return;
    home=await api(a,{target:id});searchResult=null;closeModal();signature='';renderHome();updateBadge();toast(a==='request'?'친구 요청을 보냈어요.':a==='acceptFriend'?'이제 함께 대전할 수 있어요.':'변경했어요.');
   }else if(a==='challenge'||a==='rematch')await challenge(a==='rematch'?(game.host.id===game.me?game.guest.id:game.host.id):id);
   else if(a==='invite'){const g=await api('invite',{target:id,source:$('fbSource').value,start:Number($('fbStart').value),end:Number($('fbEnd').value),mode:modal.querySelector('[data-action="mode"][aria-pressed="true"]').dataset.id,seconds:Number(modal.querySelector('[data-action="seconds"][aria-pressed="true"]').dataset.id),count:Number(modal.querySelector('[data-action="count"][aria-pressed="true"]').dataset.id)});closeModal();stamp(g);window.scrollTo(0,0);}
   else if(a==='room'){stamp(await api('poll',{id}));window.scrollTo(0,0);}
   else if(['accept','decline','ready','answer'].includes(a)){const room=game.id;const round=game.round;root.querySelectorAll('.fb-option').forEach(b=>b.disabled=true);stamp(await api(a,{id:room,round,choice:id}));}
   else if(a==='exit')await exitGame();
   else if(a==='manage'){sheet('친구 관리','<p class="fb-sub">친구 삭제와 차단을 관리해요.</p>'+(home?.friends||[]).filter(f=>f&&f.status==='accepted').map(f=>'<div class="fb-manage-row">'+person(f)+'<div class="fb-manage-actions">'+button('삭제','remove',f.id,'fb-text')+button('차단','block',f.id,'fb-text')+'</div></div>').join('')+'<h3>차단한 친구</h3>'+((home?.blocked||[]).filter(Boolean).map(f=>'<div class="fb-manage-row">'+person(f)+button('차단 해제','unblock',f.id,'fb-text')+'</div>').join('')||'<p class="fb-note fb-manage-empty">차단한 친구가 없어요.</p>'));modal.firstElementChild.classList.add('fb-manage-sheet');}
  }catch(err){toast(err.name==='AbortError'?'연결이 늦어지고 있어요. 다시 시도해주세요.':err.message);signature='';if(active()&&game)renderGame();}
  finally{busy=false;if(el.isConnected)el.disabled=false;}
 }
 function init(){if(root)return;if(!$('mainApp')||!$('myPageScreen'))return;root=document.createElement('section');root.id='friendBattleScreen';root.className='card hidden fb-root';$('mainApp').appendChild(root);root.addEventListener('click',handle);root.addEventListener('submit',e=>{e.preventDefault();e.target.querySelector('[data-action="search"]')?.click();});
  const shortcut=document.createElement('button');shortcut.className='learning-shortcut';shortcut.id='fbShortcut';shortcut.type='button';shortcut.innerHTML='<span class="fb-cover-top"><span class="fb-cover-label">친구와 함께하는</span><span class="fb-cover-badge" id="fbShortcutCount" hidden></span></span><span class="fb-cover-title" lang="en">WORD BATTLE</span><span class="fb-cover-poster" aria-hidden="true"><img src="./images/battle-faceoff-v2.webp" alt="" width="1536" height="1024" decoding="async"></span><span class="fb-cover-footer"><span class="fb-cover-cta">대전 입장 <span aria-hidden="true">→</span></span></span>';shortcut.addEventListener('click',open);$('myPageScreen').querySelector('.learning-shortcuts').appendChild(shortcut);
  const msg=document.createElement('div');msg.id='fbToast';msg.className='fb-toast';msg.hidden=true;msg.setAttribute('role','status');document.body.appendChild(msg);const notice=document.createElement('button');notice.id='fbInviteNotice';notice.className='fb-invite-notice';notice.type='button';notice.hidden=true;notice.addEventListener('click',async()=>{const id=notice.dataset.id;try{await open();stamp(await api('poll',{id}));}catch(e){toast(e.message);}});document.body.appendChild(notice);
  let replay=false;document.addEventListener('click',async e=>{if(replay||!active()||!e.target.closest('#hsmMobileNav button,#hsmGlobalBackButton,#testMenuButton,#vocabMenuButton,#myPageMenuButton,#rankingMenuButton,#accountMenuButton,#hsmMobileMore [onclick^="hsmMobileNavigate_"]'))return;const target=e.target.closest('button');if(target.dataset.mobileMenu==='more')return;e.preventDefault();e.stopImmediatePropagation();if(busy)return;busy=true;revision++;try{if(live()&&!await exitGame())return;root.classList.add('hidden');closeModal();replay=true;if(target.id==='hsmGlobalBackButton')window.showMyPage();else target.click();}catch(err){toast(err.message);}finally{replay=false;busy=false;}},true);
  const original=window.hideAllStudentMainScreens_;window.hideAllStudentMainScreens_=function(){root.classList.add('hidden');return original.apply(this,arguments);};
  setInterval(()=>{const account=who();if(account!==owner){owner=account;revision++;home=null;game=null;searchResult=null;lastRewardSync='';signature='';closeModal();root.classList.add('hidden');lastPoll=0;updateBadge();}refresh();tick();},500);
  document.addEventListener('visibilitychange',()=>{if(!document.hidden){lastPoll=0;refresh(true);}});
 }
 window.hsmOpenFriendBattle_=open;
 window.hsmBattleInProgress_=()=>!!(active()&&live())||busy;
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
